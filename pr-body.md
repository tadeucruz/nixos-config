Automated bump of the pinned upstream dependencies.

### Changes

- **OGC kernel**: `v7.2-ogc7` → `v7.2.1-ogc2` — note: base changed to 7.2.1, validate the build before merging.

### Validation

The `eval` job in `check.yml` validates the configuration.
